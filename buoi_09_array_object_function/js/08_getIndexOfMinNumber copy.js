/**
 * Write a sub function take 1 parameter.
 * The first parameter is an Array.
 * The function must return index of minimum number
 * Note: the input is always correct.
 *
 * For example:
 * Input:
 * let arr = [-7, -10, -20, 0, 1, 2, 3, 4, -100, -30, 6, 7, 8, 9, 10, 20];
 * Ouput: 8
 */

 function getIndexOfMinNumber(arr) {
    return -1;
}
console.log("-----------------");
console.log('result: ', getIndexOfMinNumber(arr));