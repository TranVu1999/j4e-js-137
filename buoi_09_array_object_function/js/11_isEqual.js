/**
 * Write a sub function take 2 parameter.
 * The first and second argument are arrays
 * The function must return if both arrays are the same. Otherwise returns false
 * Note: the input is always correct.
 *
 * For example:
 * Input: arr1 = [1, 2, 3], arr2 = [1, 2, 3];
 * Ouput: true
 *
 * Input: arr1 = [1, 2, 3], arr2 = [];
 * Ouput: false
 *
 * Input: arr1 = [1, 2, 3], arr2 = [1, 2, 4];
 * Ouput: false
 */

 function isEqual(arrDes, arrSou) {
    return true;
}
console.log("-----------------");
console.log('result: ', isEqual(arr));