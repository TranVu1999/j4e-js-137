/**
 * Write a sub function take 1 parameter.
 * The first parameter is an Array.
 * The function must return a number that's the smallest number in given array
 * Note: the input is always correct.
 *
 * For example:
 * Input:
 * let arr = [-7, -10, -20, 0, 1, 2, 3, 4, -100, 5, 6, 7, 8, 9, 10, 20];
 * Ouput: -100
 */

// function getMinNumber(arr) {
//     if(arr.length > 0) {
//         let min = arr[0];

//         for(let i = 1; i < arr.length; i++) {
//             if(arr[i] < min) {
//                 min = arr[i];
//             }
//         }

//         return min;
//     }

//     return undefined;
// }
// console.log("-----------------");
// console.log('result: ', getMinNumber(arr));

// arr = [-7, -10, -20, 0, 1, 2, 3, 4, -100, 5, 6, 7, 8, 9, 10, 20];

// let bich = arr[0];
// for (let i = 0; i < arr.length; i++) {
//     if(bich > arr[i]) {
//         bich = arr[i];
//     }
// }

// console.log('title: ', bich);



