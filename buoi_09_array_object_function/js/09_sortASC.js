/**
 * Write a sub function take 1 parameter.
 * The first parameter is an Array.
 * The function must return an array which has an array elements in ascending
 * Note: the input is always correct.
 *
 * For example:
 * Input:
 * let arr = [ 2, 3, 4, -100, -30, 6, 7, 8];
 * Ouput: [ -100, -30, 2, 3, 4, 6, 7, 8]
 */

 function sortASC(arr) {
    return arr;
}
console.log("-----------------");
console.log('result: ', sortASC(arr));