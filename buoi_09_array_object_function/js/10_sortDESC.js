/**
 * Write a sub function take 1 parameter.
 * The first parameter is an Array.
 * The function must return an array which has an array elements in descending
 * Note: the input is always correct.
 *
 * For example:
 * Input:
 * let arr = [ 2, 3, 4, -100, -30, 6, 7, 8];
 * Ouput: [ -8, 7, 6, 4, 3, 2, -30, -100]
 */

 function sortDESC(arr) {
    return arr;
}
console.log("-----------------");
console.log('result: ', sortDESC(arr));